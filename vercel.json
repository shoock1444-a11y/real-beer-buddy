{
  "version": 2,
  "framework": "vite",
  "buildCommand": "bun run build || npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
